export interface eventsCardProps {
    event : string,
    location : string,
    weekDay : string,
    day : string,
    month : string,
    year : string,
    image : string,
}

export type eventsPropList = eventsCardProps[]